Tool: built-in ImageGen edit.

Output: `iPhone/Assets.xcassets/AppIcon.appiconset/AppIcon.png` (normalized to
1024 × 1024 RGB for the iOS asset catalog).

Prompt:

Edit this existing NullSports iPhone app icon. Preserve the exact NS monogram
shape, proportions, placement and size. Make the monogram pure white and the
ENTIRE square background solid pure black, including replacing both purple
bands at top and bottom. Clean flat high-contrast black-and-white only, no
texture, gradients, shadows, additional text, borders or rounded corners.
Output an opaque square 1024x1024 PNG app icon.
